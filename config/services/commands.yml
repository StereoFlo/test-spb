services:
  weather.command:
    class: Domain\Weather\Command\WeatherCommand
    arguments:
      - '@weather.repo'
      - '@weather.temp.repo'
      - '@weather.parser'
    tags:
      - { name: console.command }